services:
  - type: web
    name: line-photo-bot
    runtime: docker
    envVars:
      - key: LINE_CHANNEL_ACCESS_TOKEN
        sync: false          # fill in Render dashboard
      - key: LINE_CHANNEL_SECRET
        sync: false
